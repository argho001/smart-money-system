[build]
builder = "dockerfile"

[deploy]
startCommand = "python3 dashboard/live_server.py"
healthcheckPath = "/api/snapshot"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
